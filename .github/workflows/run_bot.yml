name: Schengen Visa Bot

on:
  schedule:
    - cron: '*/20 * * * *' # Güvenli olması için 20 dakikada bir çalışır
  workflow_dispatch: # İstediğinde senin de elle başlatmanı sağlar

jobs:
  check-slots:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu Cek
        uses: actions/checkout@v3

      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Kutuphaneleri Yukle
        run: |
          pip install -r requirements.txt

      - name: Botu Calistir
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          COUNTRY: "Poland"
          CITY: "Istanbul"
        run: python bot.py
